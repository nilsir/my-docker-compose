global:
  resolve_timeout: 5m

route:
  receiver: wxwork-alert
  group_by: ['alertname', 'service']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: wxwork-alert
    webhook_configs:
      - url: 'http://wework-adapter:5000/webhook'
        send_resolved: true
